import React from 'react'
import { DocsThemeConfig } from 'nextra-theme-docs'

const config: DocsThemeConfig = {
  logo: <span>Mano Machine</span>,
  project: {
  },

  footer: {
  },
}

export default config
